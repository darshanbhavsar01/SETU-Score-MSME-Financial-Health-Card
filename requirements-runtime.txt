# Slim dependency set for the Docker/Cloud Run image (CLAUDE.md §13).
#
# The served /score, /validate, /narrative, /applicants API never imports model/
# (the XGBoost ranker is a build/dev-time sanity check, not wired into serving —
# see model/train.py's docstring), so scikit-learn/xgboost/joblib are intentionally
# left out here to keep the image small and the Cloud Build fast. Dev work should
# still install the full requirements.txt (superset of this file).

numpy>=1.26,<3
pandas>=2.1,<3
pyarrow>=14
duckdb>=0.10

fastapi>=0.110,<1
uvicorn[standard]>=0.29
pydantic>=2.6,<3

google-genai>=0.3
python-dotenv>=1.0
