# SETU Score backend + datagen dependencies (Python 3.11+).
# Pinned to major/minor for reproducible demo runs.

# Data generation & analytics
numpy>=1.26,<3
pandas>=2.1,<3
pyarrow>=14
duckdb>=0.10

# API layer (used from step 5 onward)
fastapi>=0.110,<1
uvicorn[standard]>=0.29
pydantic>=2.6,<3

# ML layer (optional, step 7) — a build/dev-time sanity check, not wired into the
# served /score path, so it is NOT included in requirements-runtime.txt (Docker image).
scikit-learn>=1.4
xgboost>=2.0
joblib>=1.3

# Optional Gemini narrative (step 8, off by default). google-generativeai is fully
# deprecated (EOL) — google-genai is the current maintained SDK.
google-genai>=0.3

# Tooling / tests
pytest>=8.0
httpx>=0.27          # FastAPI TestClient transport
ruff>=0.3
black>=24.0
python-dotenv>=1.0
